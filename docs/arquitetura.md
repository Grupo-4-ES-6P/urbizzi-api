# Arquitetura (Base)

Este projeto foi zerado para servir como ponto de partida.

## Estrutura inicial

```txt
src/
  app.controller.ts
  app.module.ts
  app.service.ts
  main.ts
  modules/
  shared/
```

## Próximos passos sugeridos

1. Definir os módulos de domínio em `src/modules`.
2. Estruturar camada compartilhada em `src/shared`.
3. Implementar schemas e migrations conforme o modelo de dados.
